api test {
    record A as AProvider {
        string(30) fieldA as fieldAProvider
    }

    enum E as EProvider {
        MEMBER_1 as MEMBER_1_PROVIDER
    }
    
    operation testOperation(A): A
    
}